<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:spacing="8dp"
        android:layout_marginBottom="12dp">

        <Button
            android:id="@+id/startButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Старт"
            android:textStyle="bold"
            android:backgroundTint="#4CAF50"
            android:textColor="#ffffff" />

        <Button
            android:id="@+id/stopButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Стоп"
            android:textStyle="bold"
            android:backgroundTint="#f44336"
            android:textColor="#ffffff"
            />

        <!-- <Button
            android:id="@+id/testButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Тест"
            android:textStyle="bold"
            android:backgroundTint="#2196F3"
            android:textColor="#ffffff" />-->
    </LinearLayout>

    <!-- Блок IP адреса: занимает ровно столько, сколько нужно для текста -->
    <TextView
        android:id="@+id/tvIpAddress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="IP-адрес: Определяется..."
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Журнал событий (Лог):"
        android:textSize="14sp"
        android:layout_marginBottom="4dp"/>

    <!-- Изменено: теперь ScrollView жестко занимает ВСЕ оставшееся пространство -->
    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F5F5F5"
        android:padding="8dp"
        android:fillViewport="true">

        <!-- Внутри ScrollView у TextView ВСЕГДА должно быть wrap_content -->
        <TextView
            android:id="@+id/tvLogs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Сервер ожидает запуска...\n"
            android:fontFamily="monospace"
            android:textSize="12sp"
            android:textColor="#222222"/>
    </ScrollView>
</LinearLayout>